# CINDERFALL v4 可玩性与 AI 展示迭代方案

## 1. 目标与取舍

本项目的目标不是发布一款重度 4X 游戏，而是在 2–4 分钟内让试玩者同时感受到：

1. 我能看懂并做出一两个有意义的策略选择；
2. AI Director 会基于已经发生的玩家行为，重新组织地图上的机会；
3. AI 不会偷看未提交指令、不会给任意一方作弊，也不会制造无解局面。

因此优先级为：**可理解的可玩性 > AI 影响的可见性 > 策略深度 > 规则完整度**。完整策略规则应保留为后续深度，而不能挡在首局之前。

## 2. 当前体验结论

### 已有优点

- 双路线、Relay、补给切断和 Beacon 形成了清晰的中局目标，不是纯扩张。
- 战斗没有随机骰子；攻击、地形和协同关系可计算，适合展示“AI 在公平规则内行动”。
- Director 已有候选生成、反事实模拟、LLM 选择、代码校验与兜底，架构质量高于普通随机事件系统。

### 当前最大问题

- 首局同时出现预算、两次行动、路线承诺、储备、组合支援、补给、Relay、Beacon 和世界事件，信息量过高。
- 盘面上的可点击目标只有细虚线；不可点时信息出现在下方记录中，玩家很难建立“为什么不能点”的因果。
- 25 回合对展示型 Demo 偏长，玩家可能还未理解 AI 的作用就进入重复扩张。
- Director 的事件很多，但“为何因为我的打法而出现这个资源机会”的表达还不够聚焦。

## 3. 用户试玩反馈与原因定位

| 反馈 | 实际规则 | 体验问题 | 本轮处理 |
| --- | --- | --- | --- |
| 开局有 8 点，点两块地后花了 6 点，剩余点不能继续操作 | **旧版** Claim 各消耗 2，第二次 Move 另收 2 点动员费；每回合最多两次行动 | 第二次 Move 的额外税没有产生策略，只让 Supply 看起来像“点不了的行动点” | **已改**：Claim / Hold / Attack 为 3 / 2 / 4；两次 Move 不再另收费；未花 Supply 1:1 存入 Reserve，或可立即花 2 做 Combo Boost |
| 不理解地块含义，也不清楚为什么不能点 | 合法目标受相邻、所属、补给、路线锁和两次 Move 限制 | 合法轮廓过弱；无效点击只在日志里解释 | 加粗并动画化合法目标的虚线、增加地图图例、悬停明确说明“为何不可点” |
| 希望有 LLM 驱动的测试玩家 | 目前只有 Saltkin 和 Director 使用 LLM | 难以快速观察不同打法与数值平衡 | 新增 Mock Player：LLM 选打法，规则 Bot 生成合法 Ashfarer 队列并记录来源 |
| 对战记录难读 | 当前记录混合叙事、技术状态和事件文本 | 玩家难从长句中识别“谁做了什么、结果如何、AI 为什么介入” | 本轮先补 Mock 结果记录；下一轮应将记录拆为“行动 / 结果 / AI 解释”三层 |

## 4. 本轮模拟证据

### 大型优化复测（当前版本，2026-08-08）

本轮以“第一次操作以后，玩家能否清楚看懂自己的选择；世界变化是否真的创造新选择”为核心，完成两次实现与复测。第一版只提高 Bloom / 新岛权重，虽然资源事件出现了，却落在不够可争的位置，导致十局 Raid 从 18.1 降到 12.4；因此**没有作为交付版本**。第二版将第一次世界响应锁定为中部新岛、第二次优先中部 Bloom，并同步缩短路线承诺和补足地图反馈，才作为当前版本。

#### 已实现的玩法与 UI 改动

1. **路线选择从隐性规则变为盘面状态。** 地图顶部固定显示北路、底部固定显示南路；开局加成路线会高亮；第一次行动后对应路线直接显示“已选择主攻”。行动栏同步写明“另一路线锁定至第几回合”，不再让玩家从日志猜原因。
2. **主攻路线由 3 回合缩短为 2 回合。** T1 选择路线只要求承诺 T1–T2；T3 世界第一次响应时，玩家已经有资格为新机会重新部署。换线占用 2 次行动，但仍保留 1 次行动，因此有代价但不会把新手困在错误路线太久。
3. **前两次世界响应优先机会而非灾害。** 第一次优先在中央生成双方可达的新岛；第二次优先中央 Bloom。领地差 ≥7 或收入差 ≥5 时，资源机会的权重进一步提高，但不直接给落后方数值补偿。
4. **事件仍必须经过完整护栏。** 所有事件继续经过候选模拟、25% 单边损失上限、最低三格、Capital / 路径保护、连续受害方限制与确定性 fallback；模型不能直接指定地图坐标或绕过校验。
5. **Director 的实时等待设置为 5 秒交互预算。** 超时立即采用已模拟、已验证的安全基线继续结算；迟到的模型回复会被丢弃，不能回写已经进入下一回合的地图。玩家层只看到世界观内的“山脉正在读取战局 / 选择最安全路径”，技术原因仅留在 Cinder Console。

#### 行动经济重构：2 → 3 次行动（当前版本）

**结论：保留行动上限，但由 2 次上调为 3 次；Supply 成为主要约束。** 完全删除上限会让高收入与 Reserve 在后期把一次回合变成连续点格，地图对抗会被经济滚雪球淹没。三次行动则刚好放开开局的 `3 + 3 + 2 = 8 Supply`：玩家可选择 `两次扩张 + 一次固守`，或 `两次扩张 + 连携`，而不是被迫在两次扩张和固守之间二选一。

| 指标 | 原 2 次行动（100 局） | 3 次行动（100 局） | 解释 |
| --- | ---: | ---: | --- |
| 胜率 | Ash 51% / Saltkin 44% / Draw 5% | Ash 51% / Saltkin 44% / Draw 5% | 放宽操作没有形成阵营偏置。 |
| Raid | 20.5 / 局 | **30.3 / 局** | 接敌后的可选组合更多，防守不再天然占节奏。 |
| 协同进攻 | 5.0 / 局 | **6.1 / 局** | 双向进攻更常形成，玩家更容易看见战术回报。 |
| 静默回合 | 13.4 / 局 | **10.6 / 局** | “只点地块”的空白回合明显减少。 |
| 平均时长 | 22.9 回合 | 22.6 回合 | 节奏更活跃，但没有压缩成过快的短局。 |

最终 10 局复测为 5 胜 / 5 胜、平均 20.5 回合、27.5 次 Raid、9.4 个静默回合；所有地图、补给、路线、事件、候选和回放不变量通过。HUD、引导、AI prompt、README 与测试均已改为“3 次行动”。

#### 最终十局结果

| 指标 | 修改前基线 | 第一轮（未采用） | 当前版本 | 结论 |
| --- | ---: | ---: | ---: | --- |
| 胜负 | Ash 4 / Saltkin 5 / Draw 1 | Ash 5 / Saltkin 5 / Draw 0 | Ash 3 / Saltkin 7 / Draw 0 | 十局受种子影响大；需结合 100 局判断阵营平衡。 |
| 平均时长 | 23.1 回合 | 22.1 回合 | **19.8 回合** | 完整局更紧凑，展示中更容易在注意力耗尽前出现胜负。 |
| Raid | 18.1 / 局 | 12.4 / 局 | **14.5 / 局** | 比基线略低，但第一轮回落已被修正；配合更少静默回合，操作反馈更连续。 |
| 静默回合 | 14.7 / 局 | 15.6 / 局 | **12.7 / 局** | 明显下降，地图不再长时间只是占地。 |
| 资源机会 | Bloom / 新岛 0 | Bloom 10 / 新岛 1 | **新岛 10 / Bloom 6** | 已从“机制摆设”变为每局可观察的核心变化。 |
| 补给切断 | 10 / 10 | 8 / 10 | 8 / 10 | Relay 仍然是有效目标，需后续补强断供的视觉链路。 |

为避免十局偶然性掩盖问题，另运行 100 局相同确定性模拟：Ashfarers 51 胜、Saltkin 44 胜、平局 5；平均 22.9 回合、20.5 Raid、13.4 静默回合、7.0 次事件。结论是**没有固定阵营偏置**，资源事件也没有破坏安全性或可达性。当前版本不是“已经完全平衡的发布游戏”，但已满足展示 Demo 所需的可玩、可解释和公平底线。

#### 本轮验证

- `node tools/simulate.js 10 verbose`：全部 37 项地图、经济、路线、补给、事件、候选和回放不变量通过。
- `node tools/simulate.js 100`：用于验证胜率与资源事件分布，不替代真人试玩。
- `python3 -m unittest discover -s tests -v`：27 项服务、隐私边界、HTTP、静态资源和交互约束测试全部通过。
- 页面核验：中文状态下可同时看见 `北路 / 可选战线`、`南路 / 开局肥沃加成` 与“第一次扩张锁定路线 2 回合”的行动提示；地图标签不会遮挡点击。

### 本轮 10 局规则模拟（改后）

- 结果：Ashfarers 4 胜、Saltkin 5 胜、平局 1 局；小样本内没有阵营单边碾压。
- 平均：23.1 回合、18.1 次 Raid、3.2 次协同进攻、14.7 个低冲突回合、7.2 次 Director 事件。
- 所有地图对称性、补给、Relay、Beacon、事件安全性、无双灭、LLM 候选校验和回放一致性检查均通过。
- 10 局均出现过补给切断，说明 Relay 目标确实进入了对局；但低冲突回合仍偏多，Showcase Run 应强制在第 3 回合展示一次 AI 资源重组，而不是等待自然触发。

### 本次 10 局复测：节奏、可玩性与 Director 等待（2026-08）

本次使用 `node tools/simulate.js 10 verbose`，只运行与正式游戏相同的确定性地图、经济、Bot、事件与安全校验；**不调用外部 LLM**，因此它用于衡量规则节奏，而非云端响应时间。

| 观察项 | 复测结果 | 体验解读 | 下一步优化 |
| --- | --- | --- | --- |
| 胜负 | Ashfarers 4 胜 / Saltkin 5 胜 / 平局 1 局 | 小样本内没有固定阵营优势，但不能据此宣布最终平衡 | 保留 100/400 局长期监测；展示时不以 10 局胜率作为结论 |
| 对局长度 | 平均 23.1 回合；7/10 对局打满 25 回合 | 完整版偏长，试玩者在理解核心机制前容易进入重复占地 | Showcase Run 固定为 8 回合，完整版保留 25 回合 |
| 冲突密度 | 平均 18.1 次 Raid，但 14.7 个回合无 Raid（约 64%） | 大量回合缺少对抗反馈；玩家会觉得“只是点地块” | 更早提示 Relay、Beacon 与压力桥的可争夺价值；将首个 Cinder 事件设为资源机会而非纯灾害 |
| Relay 影响 | 10/10 局出现补给切断 | Relay 是有效核心目标，但首次断供对新玩家可能像突然惩罚 | 在敌方接近两格 Relay 时预览“将断供哪些格/Beacon”，先教会风险再结算后果 |
| 波动 | 3/10 局出现 20 格以上领地差（如 4:25、2:28、30:3） | 领先方滚雪球仍过快；即使最终胜率接近，单局观感会很挫败 | 当领地差达到 7 时，只生成双方等距、可争夺的 Bloom / 新岛 / Crossing，不直接给落后方数值补偿 |
| Cinder 事件 | 平均 7.2 次 / 局；Storm 17、Fire Moves 14、Eruption 12；Bloom 与新岛 0 | 事件很勤，但以打断为主，尚未突出“AI 重组资源机会” | 首两次事件优先从 Bloom / 新岛 / Beacon Shift / Crossing 选择；破坏性事件留给僵局或大幅领先时 |
| 预测 | 62 次已验证预测中 22 次成立（35%） | 预测不适合包装成准确承诺；它的价值是说明“为什么现在给出这个机会” | 玩家层展示“观察 → 新机会 → 公平边界”，预测命中率只放审计区 |

#### Cinder 长时间 `Considering possible futures` 的定位与时限规范（P0）

当前等待不是本地候选模拟造成的：上述 10 局完整规则模拟只耗时约 **401ms**（约 40ms/局）。页面卡住的来源是 Cinder 每逢第 3、6、9…回合向云端模型请求候选选择；当前本地服务健康检查显示 `CINDERFALL_AI_TIMEOUT_SECONDS=45`，浏览器还会额外预留约 1.5 秒接收服务端的降级结果。因此网络慢或模型排队时，单次等待可接近 **46.5 秒**，一场约 23 回合的完整对局理论上会累积多次长等待。

当前实现与后续迭代均须遵守以下交互预算（第 1、3 项已落地）：

1. **玩家回合不可被 Director 请求长时间冻结（已完成）。** Cinder 在后台读取公开战局；超过 5 秒即采用已验证的确定性候选继续游戏，迟到回复被安全丢弃，不回写已开始的回合。
2. **Showcase 只展示一次可见的实时模型决策。** 建议在 T3 触发；其余事件由已经展示过的同一套候选/护栏逻辑快速落地，避免一次试玩被 6–8 次网络等待切碎。
3. **玩家层不出现 “AI Director is considering” 等技术描述（已完成）。** 等待状态使用世界观内的短句，例如“山脉正在读取战局”；若进入快速规则方案，则写“Cinder 选择最安全的路径”。模型延迟、超时和 Fallback 仅在 Cinder Console 的审计区出现。
4. **演示配置建议。** 将外部模型响应预算单独收紧至 4–5 秒，并在开场预热一次连接；完整 45 秒超时仅适合开发排障，不适合实际试玩。

### 结合此前 100 局抽样

- 胜率为 Ashfarers 39%、Saltkin 48%、平局 13%，没有稳定阵营偏置。
- 平均 23.1 回合、15.8 个无 Raid 回合、7 次事件。
- “Fire Moves / Storm / Eruption”出现频率明显高于 Bloom，现有 Director 更擅长打破局面，而较少展示“资源重分配”。

**结论：** 数值逻辑本身可运行，但其说明与反馈不足；同时应让 Director 更经常选择玩家可见、可争夺、非纯破坏的资源机会。

## 5. 已落地改动

### 首局与数值解释

- 初始 LLM Doctrine 后台请求，不再冻结首个可玩时刻。
- 引导从 6 屏压缩为 3 屏：`抢地 → Relay / Beacon → Director 公平性`。
- 动作术语调整为 `Claim / Hold / Attack`，`Field Commands` 调整为 `Moves`。
- 两次 Move 不再有隐藏动员费；未花 Supply 1:1 存入 Reserve（上限 8）。
- 首局双 Claim / 双向 Attack 成立时才显示 Combo Boost：玩家可花 2 Supply 立即强化，也可把这 2 点存入 Reserve。

### 地图可操作性

- 可操作地块改为更粗、更亮、会流动的虚线框。
- 盘面左下增加图例：发光边框 = 可行动、◇ = Relay、★ = Beacon。
- 悬停不可操作格会直接说明原因，例如“不是相邻空地”“两次 Move 已用完”“该格未被补给”。

### Mock Player

- Console 新增 `MOCK LLM MOVE`。
- 调用与当前配置相同的 LLM API，但使用 Ashfarers 专用 prompt。
- 模型只返回 Doctrine（姿态、目标、方向、风险和可解释意图）。
- 确定性 Bot 再生成真实、可负担、合法的点格操作；失败时明确标记 fallback，不伪装成 LLM 成功。
- 记录会写出：模型来源、Doctrine、意图和已排队动作，便于对照对局。
- 实测当前 `moonshotai/Kimi-K2.7-Code` 的一次 Mock 返回约 **33.9 秒**。现场演示应提前预热一次，或将按钮文案改为“生成 AI 方案”并保留明确的加载状态；不要让主游戏回合依赖该等待时间。

## 6. 下一轮玩法改造建议

### P0：先修复“等待打断试玩”的展示节奏

- 按上述 5 秒预算让 Cinder 的模型请求后台化，并确保超时后立即使用确定性安全候选；对局不能因网络质量停摆。
- Showcase 采用 **一次实时 AI 决策 + 两次快速世界响应** 的节奏：T3 展示真实适配，T5/T7 用已校验规则迅速兑现地图变化。
- 首次 Cinder 响应必须制造可见、双方等距的资源目标；将“火山纯破坏”延后，先让玩家感受到“我的打法改变了下一处值得争夺的资源”。
- 当任意一方领先 7 格以上时，优先投放中立、可双向到达的机会点并清楚标注路径；这不是补偿，而是给落后方和领先方同一份新决策。

### P0：做一个独立 Showcase Run

保留 25 回合完整版，同时新增 8 回合 Showcase Run：

1. T1–T2：只教 Claim，系统高亮两条可走路线。
2. T3：Director 读取“偏好北路 / 偏好扩张 / 偏好防守”等公开行为，生成一个双方等距可争夺的资源机会。
3. T4–T5：玩家选择继续原路线，或转去新机会；地图明确显示选择代价。
4. T6：展示一次 Relay 断供或 Beacon 无补给不计分。
5. T7–T8：Director 总结它如何适配玩家，并展示安全护栏通过情况。

Showcase 的目标不是严肃胜负，而是让评委在一次试玩内看懂 AI 导演的输入、决策、影响和公平边界。

### P1：收敛 Director 的首局事件库

首局只允许三类“机会型”事件，其余破坏事件留给完整版：

- **Bloom Route**：在玩家忽略的路线生成双方可到达的高肥沃资源带。
- **Crossing Window**：僵局时开放两回合临时中路，让防守方和进攻方都能选择换线。
- **Beacon Shift**：Beacon 移到未被垄断、双方路径接近的位置。

这些事件应在 UI 中固定显示：`观察到的行为 → AI 目标 → 双方获得的机会 → 护栏结果`。不要只显示灾害名称。

### P2：统一行动和 Supply 的心智模型

- HUD 拆成两行：`3 次行动` 与 `Supply Budget`；Supply 是主要预算，三次行动只防止高收入局面一次性连点过多。
- 第二 Move 不应收费；Move 是操作上限，Supply 是对动作和强化的预算。
- Move 满后将未花 Supply 显示为 `Will bank: 2 Supply → 2 Reserve`，不要只保留一个数字。
- 首局不显示 Combo Boost；在首次连续 Claim 或双向 Attack 成功后再用一次性教学提示解锁。

### P3：重写对战记录

每回合固定三段，每段至多一行：

1. **You / Rival**：`Claim C4 + Claim D4`；
2. **Result**：`双方各扩张 2 格，北路距离 Relay 还差 3 格`；
3. **世界变化**（仅触发时）：`你连续 2 回合偏向北路；山脉在中央催生双方可争的 Bloom。`

技术性完整日志继续保留在 Chronicle / Console，不占用首局阅读注意力。

## 7. 比赛评分对应关系

| 评分项 | Demo 应展示的证据 |
| --- | --- |
| Problem–Solution Fit 40% | 静态地图无法适配激进、扩张、防守等玩家风格；Director 用已发生的公开行为重新分配机会，而不是暗改胜率。 |
| Build Quality 30% | LLM 真正选择 Doctrine / 事件候选；确定性代码负责合法订单、预算、反事实模拟、验证器和 fallback。Mock 按钮可现场复现。 |
| Originality 30% | 不是 Rule-Based 概率事件：系统有玩家画像、候选方案、对局模拟、LLM 比较、解释文本和公平护栏。 |

## 8. 验收标准

- 新玩家在 30 秒内能说出“我一回合只有两次 Move；发光框可以点；Relay 会断补给；Beacon 要有补给才得分”。
- 玩家第一次花完 6 / 8 Supply 后，能看懂为何不能点第三格，以及剩余 2 Supply 如何处理。
- 点击 Mock 后，界面在成功时明确显示模型、Doctrine 与合法队列；失败时明确显示 fallback。
- 每个 Director 事件都能回答：它观察到了什么、改变了什么、双方为什么仍有公平选择。
- Showcase Run 在 8 回合内至少展示一次资源机会重组和一次安全护栏说明。

## 9. 玩家一分钟试玩策略

### 先只记住四条

1. 每回合最多 **2 次 Move**；先点 `Claim`，发光虚线格就是当前可点目标。
2. `Supply` 不是额外 Move：Claim / Hold / Attack 分别花 **3 / 2 / 4**；没花的 Supply 会 1:1 存为 Reserve。
3. 两个 Claim 连成一线后，出现 `Claim Combo +2`：花掉可以让第二格更耐打；不花则把 Supply 留给下回合的 Attack。
4. Relay（◇）比普通地块重要：拿下一个方向的两个 Relay 格，可能切断对方收入和 Beacon 补给。Beacon（★）只有保持补给才计分。

### 建议按这个顺序试玩

- **T1**：开局导向路线提供 8 Supply。点两次 Claim 后剩 2：优先沿 `OPENING BONUS` 指向的路线走；第一次可选 Claim Combo 看“强度 2”的推进，下一局可不点、把 2 Supply 存成 Reserve，直观看到两种打法的区别。
- **T2**：继续同一路线，目标不是随便抢肥沃地，而是靠近 Relay 或 Beacon。路线仅锁定两回合；T3 起可以为了世界新机会进行一次完整的重新部署。
- **接敌后**：先用 Hold 把临近敌人的格升强；若同一敌格能从两个己方格攻击，两个 Attack 会获得 +2 协同，优先尝试它。
- **Director 提示出现后**：看它写的“观察到的行为 → 提供的新机会”。它通常不是让你立刻照做，而是给你“继续原计划”或“改去争新资源”的选择。

### 三种容易上手的打法

| 打法 | 怎么做 | 适合观察的 AI 效果 |
| --- | --- | --- |
| 稳定扩张 | 前两回合都 Claim 开局路线，存 Reserve | 世界是否在中部制造双方可争的新资源 |
| Relay 突击 | 沿最近 Relay 推进，接敌后双向 Attack | 补给被切断时，地图和收入如何变化 |
| Beacon 守持 | 拿 Beacon 后用 Hold 守住补给路线 | Beacon 失去补给时为何停止得分，Director 如何反应 |

## 10. 开源策略游戏参考：可吸收内容与边界

本节只吸收已经被验证的**交互与信息设计原则**，不复制其他项目的代码、美术、规则文本或复杂系统。v4 的核心仍是“AI Director 根据公开玩家行为，公平重组地图机会”的轻量对抗 Demo，而不是缩小版 4X 或完整战棋。

### 10.1 参考定位

| 参考方向 | 适合 v4 吸收的原则 | 本项目中的落点 | 明确不引入 |
| --- | --- | --- | --- |
| [Athena Crisis](https://athenacrisis.com/open-source/)：现代回合制格子策略 | 先选操作、再用高亮告诉玩家哪里可走；目标清晰且以短关卡承载 | Claim / Hold / Attack 只在选中后高亮对应格；Showcase Run 每回合只教一个目标 | 多兵种、技能树、长战役、复杂生产 |
| [Battle for Wesnoth](https://github.com/wesnoth/wesnoth)：据点收入与地形战术 | 据点、收入、状态与地图格信息必须能即时读懂 | Relay 固定显示“贡献的 Supply / 断供范围”；悬停格显示归属、强度、补给、收益、不可操作原因 | 兵种克制、昼夜、随机命中、单位养成 |
| [Mindustry](https://github.com/Anuken/Mindustry)：资源网络的可见因果 | 网络被切断时，玩家应一眼看到链路、影响范围与后果 | Relay 至 Beacon 的补给线常驻显示；切断后红色断线、灰化失供格、收入变化和 Beacon 停分同时出现 | 实时自动化、采集链、建筑配方、海量物品 |
| [Freeciv-web](https://github.com/freeciv/freeciv-web)：领土与资源的宏观目标 | 资源点和领土需要明确“为什么值得争夺” | 每个 Relay / Beacon / AI 新机会固定标出收益与争夺风险；Director 只改变机会，不直接改变胜负 | 城市、科技、外交、文明级长线经营 |

### 10.2 下一轮必须落地的四项体验改造

#### A. 操作模式与合法格同屏绑定（Athena Crisis 原则）

- 选 `Claim` 后，除合法空格外其余地图降到 45% 明度；选 `Hold` / `Attack` 时同理。
- 合法格使用实线外框加脉冲，不再只依赖虚线；不可操作格悬停时显示一句原因，不打开长日志。
- 订单区同步给出计数，例如：`Move 1/2 · Supply 3/8 · 下一步：选择一个发光格`。
- 首次进入 Showcase 时，默认只开放 Claim；第二回合才展示 Hold；第一次接敌才展示 Attack 与协同。

**验收：** 新玩家不读规则，也能在 5 秒内知道“当前可点哪里、点下去会花什么”。

#### B. Relay 补给链可视化（Mindustry 原则）

- 在己方 Capital → Relay → Beacon 之间渲染细的常驻补给线；线条只表达连通关系，不新增资源玩法。
- 一组 Relay 被夺取后，立即播放 `CUT OFF`：被影响格灰化，Supply 预测数先变更，Beacon 图标熄灭，最后在 Chronicle 写结果。
- Relay 悬停卡固定四行：`本格 Supply`、`维持 Beacon`、`切断影响`、`双方到达距离`。
- Director 放置 Bloom / Crossing / Beacon Shift 时，先高亮它改变的“可达路径”，再让玩家选择；不要先播灾害特效。

**验收：** 玩家能在 3 秒内回答“我为什么要抢这个 Relay；失去它后具体少了什么”。

#### C. 8 回合 Showcase 关卡节奏（Athena Crisis + Wesnoth 原则）

| 回合 | 只向玩家展示的事情 | 目标 |
| --- | --- | --- |
| T1 | Claim + 发光路径 | 完成两次扩张，并决定把最后 2 Supply 用于固守或连携 |
| T2 | Supply 8 = 双 Claim + Combo / Reserve | 做一次可见的资源取舍 |
| T3 | Relay 与补给线 | 争夺一个双方等距的 Relay |
| T4 | AI 行为观察卡 | 看见“你偏北路 / 你偏扩张”等公开画像 |
| T5 | Bloom 或 Crossing Window | 看到 AI 创造的公平替代机会 |
| T6 | 断供或 Beacon 停分 | 看懂网络后果 |
| T7 | 双向 Attack 或 Hold | 完成一个中局战术选择 |
| T8 | Director 总结与公平护栏 | 复盘“观察 → 机会 → 玩家选择 → 验证” |

#### D. AI Director 的可解释机会卡（v4 差异化核心）

每次 Director 介入只出现一张固定格式的机会卡：

```text
OBSERVED     你连续 2 回合推进北路
INTENT       避免南路长期无人争夺
CHANGE       南路 Bloom：双方到达距离均为 2
YOUR CHOICE  继续北路夺 Relay / 改争 Bloom
FAIRNESS     对称可达 · 未读取未提交订单 · 预测已校验
```

- LLM 只负责基于公开局势选择“为何提供哪一种机会”；地图坐标、距离对称、资源数值、合法性与反事实验证均由确定性规则生成和校验。
- 机会卡必须在地图变化前出现；玩家可选择继续原计划，因此 AI 是适配器而不是导演式强制脚本。
- Showcase 优先使用 Bloom / Crossing / Beacon Shift 三类机会型事件；破坏性事件移至完整版的中后期。

### 10.3 设计边界与叙事表达

- 对外表述为“受到经典回合制策略、据点经济和可视化补给网络的启发”，不要声称或暗示复刻某个作品。
- 项目只借鉴玩法思想；如未来引入第三方代码、素材或地图，必须单独审查其许可证并保留归属说明。
- 比赛展示时，先让评委玩完 Showcase 的 T1–T5，再展示 LLM 请求、候选比较、验证器和 Mock Player；先证明好玩与可理解，再证明 AI 的真实参与。
- **玩家可见文案边界（新增，P0）**：游戏引导、战报、地图提示、事件卡、结算提示与普通游戏日志必须完全处在 `CINDERFALL` 世界观内。用 **Cinder / 山脉 / 风暴 / 盐潮军 / 世界事件** 描述系统行为，不出现 `AI Director`、`LLM`、`模型`、`比赛 Demo`、`候选模拟`、`调用` 等制作层或参赛层措辞。真实 AI 机制、模型来源、请求状态、候选对比与安全护栏仅放在明确标记为开发/审计用途的 **Cinder Console**，并仅在比赛讲解阶段主动打开；不能让普通玩家误以为自己正在操作一份技术演示。

## 11. 引导规则与实战地图的 UI 对应问题（待改）

### 11.1 当前核查结论（P0 已完成）

北路、南路、横桥、Relay、Beacon 和 Opening Bonus 的**规则逻辑**已与引导一致。本轮已补齐最关键的路线视觉化：上方为北路、下方为南路，开局加成和已选择主攻会在对应路线标签上高亮；行动栏会同时解释锁定剩余时间。玩家不再需要从英文日志推断自己是否选了北路。

| 引导中的概念 | 实际规则对应 | 当前 UI 问题 | 必须补的反馈 |
| --- | --- | --- | --- |
| 北路 | 地图上方两行主战线 | **已解决** | 上边缘固定显示 `北路 / NORTH ROUTE`；选定后显示“已选择主攻” |
| 南路 | 地图下方两行主战线 | **已解决** | 下边缘固定显示 `南路 / SOUTH ROUTE`；开局加成时显示高亮状态 |
| Opening Bonus | 首局随机指定北或南路线，持续至 T4 | **已解决（文字与高亮）** | 对应路线标签显示流光与 `开局肥沃加成`；T4 后淡出 |
| 横桥 | 左右两侧的中部连接区，可换线切后方 | 看得见地块相连，不知道它具有“换线”意义 | 在两端桥口显示低调 `CROSSING` 标识；首次可达时播放一次“可包抄”提示 |
| Relay | 四组两格的补给中继节点 | 只显示 `◇`，不知道哪两格属于同一 Relay、断供影响多大 | 标出组名与连线；悬停明确显示“拿下此组将切断 X 格 / Beacon 补给” |
| Beacon | ★ 所在地块，需保持补给才得分 | 图例说明了得分，地图未说明与补给线关系 | Capital → Relay → Beacon 常驻细补给线；断供时线断开、Beacon 熄灭 |

### 11.2 推荐实现顺序

1. ~~**P0：路线标识与 Opening Bonus 指向。**~~ **已完成**：首局已能回答“我现在选了哪一路”。
2. **P1：Relay 分组与断供预览。** 解决“我为什么要抢这里”。
3. **P2：补给线与 Beacon 熄灭反馈。** 解决“抢到 / 丢掉以后发生了什么”。

### 11.3 Combo 按钮的当前规则与位置

| 项目 | 当前设计 |
| --- | --- |
| 位置 | `指令 / ORDERS` 面板中，三张 `Claim / Hold / Attack` 操作卡的正下方、`QUEUED` 队列的正上方。移动端需要向下滑到三张操作卡之后。 |
| 按钮名称 | 连续扩张时为 `CLAIM COMBO · +2`；双向进攻时为 `ATTACK COMBO · +2`。 |
| 出现条件 | 本回合已经排入两次**连续 Claim**，或两次从不同己方格发起、指向同一敌格的 Attack。 |
| 费用与效果 | 再花 2 Supply；Claim Combo 让第二格以强度 2 建立，Attack Combo 为协同进攻追加强化。 |
| 不出现 / 不可用原因 | 两次操作尚未形成 Combo、剩余 Supply 少于 2、或还在首两次操作之前。 |

**UI 改进要求：** Combo 不能只在操作卡下方“悄悄出现”。当第二次有效操作形成 Combo 时，应从对应第二张订单位置弹出 `COMBO READY · Spend 2 or bank it` 提示，并用箭头指向 Combo 按钮；若 Supply 不足 2，则显示禁用原因 `还差 1 Supply`，而不是只保留一个灰色按钮。
