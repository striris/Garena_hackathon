# CINDERFALL 精简规则改版规格（提案 v0.2）

状态：讨论稿，不对应当前代码。

## 1. 设计结论

本版不检测、不定义也不针对某一种“僵局”。它通过修改基础玩法，让重复交换和“打成中立后再扩回”的循环不再成立。

删除：

- Relay、补给连接和断补给；
- 地块强度、高地加成和战斗公式；
- 永久要塞与 Fortify；
- 肥沃地、补给令牌和 Overcharge；
- 攻击后变为中立的过渡状态；
- Cinder 的地形破坏、造地和削弱事件；
- 静止回合、重复局面和 Pressure 等反僵局触发器。

保留：

- 环形地图上的南北双战线与跨火山桥；
- 每回合两个秘密命令；
- 领土推进、Beacon 得分与首都胜利；
- Saltkin 的公开 AI 意图；
- Cinder 提前公布的 Beacon 迁移。

## 2. 游戏目标

- 玩家控制西侧 Ashfarers，AI 控制东侧 Saltkin。
- 率先获得 4 点 Beacon 分数者获胜。
- 占领敌方首都者立即获胜。
- 对局最多 10 回合。
- 第 10 回合结束仍无人立即获胜时，依次比较 Beacon 分数、控制地块数；仍相同则平局。

已获得的 Beacon 分数不会失去。即使地图完全没有变化，只要一方控制 Beacon，对局也会继续走向结束。

## 3. 地图与地块

- 地图继续使用南北两条主路线组成的环形结构，并保留跨火山桥。
- 邻接只计算上下左右，不计算斜角。
- 每块陆地只有三种所有权：中立、Ashfarers、Saltkin。
- 地块没有强度、等级、补给或永久防御状态。
- 任何己方地块都可以参与行动。
- 首都是特殊目标，但不使用另一套地块数值。

## 4. 每回合流程

双方每回合各秘密提交恰好两个命令，然后同时公开。

三种命令：

1. Expand：占领中立地；
2. Raid：直接夺取敌方地；
3. Guard：保护己方地一回合。

结算顺序：

1. 统计 Guard；
2. 同时结算所有 Raid；
3. 分层结算所有 Expand；
4. Beacon 计分；
5. 到期的 Beacon 迁移；
6. 检查胜负并进入下一回合。

## 5. Expand

Expand 的目标必须是与己方地块相邻的中立陆地。

成功后目标直接变为己方领土。

同一方可以用两个 Expand 连续推进两格：

```text
己方 → 中立 A → 中立 B
```

第二步依赖第一步。第一步失败时，第二步也失败。

若双方在同一扩张层级选择同一个中立目标：

- 双方命令互相抵消；
- 目标保持中立；
- 不比较兵力、邻接数量、领土总数或随机先手。

## 6. Raid

Raid 的目标必须是与至少一块己方地相邻的敌方地块。

普通地块受到至少一次未被 Guard 抵消的 Raid 时，直接变为进攻方领土。它不会先变成中立地，因此不存在下一回合用 Expand 抢回原地的固定循环；原主人若想夺回，必须使用 Raid。

同一方可以把两个 Raid 投入同一目标。多出的 Raid 主要用于突破 Guard 或攻击首都。

Raid 阶段读取回合开始时的地图快照并同时应用：

- 被攻击地块本回合仍可作为原阵营其他 Raid 的邻接依据；
- 本回合刚夺取的地块不能继续作为第二次 Raid 的跳板；
- 双方可以在不同位置同时成功夺地；
- 双方对彼此相邻地块同时 Raid 时，两个结果都执行，可能交换这两个地块的所有权；这需要双方主动同时投入命令，不会产生白地—Expand 循环。

## 7. Guard

Guard 的目标必须是回合开始时属于己方的地块。

一个 Guard 抵消该目标本回合受到的一个 Raid：

```text
有效 Raid 数 = 敌方投入的 Raid 数 - 己方投入的 Guard 数
```

- 有效 Raid 数至少为 1：普通地被夺取；
- 有效 Raid 数为 0：地块保持不变。

同一目标最多安排一个 Guard。Guard 只持续当前回合，不在地图上留下永久要塞。

因此基本攻防只有三种结果：

| 对普通地的投入 | 结果 |
|---|---|
| 1 Raid，0 Guard | 攻方夺取 |
| 1 Raid，1 Guard | 防守成功 |
| 2 Raid，1 Guard | 攻方夺取 |

玩家不需要记忆攻击值或防御值，只需数命令。

## 8. 首都

- 首都需要同一回合累计 2 个有效 Raid 才能被占领。
- 一个 Guard 可抵消其中一个 Raid。
- 因此防守中的首都不会被两个 Raid 攻下：`2 Raid - 1 Guard = 1`，未达到 2。
- Guard 下一回合失效，守方必须决定是否继续投入命令保护首都。
- 前两个回合禁止以首都为 Raid 目标。
- 双方首都若在同一 Raid 阶段同时失守，则为平局，并停止该回合后续结算。

首都规则让斩首需要明确的双命令投入，也让持续守都产生真实机会成本。

## 9. Beacon

- 地图上始终只有一个 Beacon。
- Beacon 计分阶段结束时，其所在格属于哪一方，该方就获得 1 分。
- Beacon 位于中立地时无人得分。
- 占领 Beacon 不立即加分，必须等到计分阶段。
- 率先达到 4 分者获胜。

界面始终显示：

> 若局面不变，本回合 Ashfarers +1。

Beacon 的持续计分和 10 回合上限保证游戏必然结束，不需要额外的反僵局判定。

## 10. Cinder：只移动 Beacon

Cinder 不再改变地形或战斗状态，只负责选择 Beacon 的下一个位置。

- 第 2、4、6、8 回合开始时公布新位置；
- 双方在知道新位置的情况下进行整个回合；
- 当前 Beacon 先完成本回合计分；
- 随后移动到已公布位置；
- 新位置从下一回合开始计分。

程序生成 3 个合法候选，大模型只能从中选择一个。候选必须：

- 不是首都，也不是当前 Beacon；
- 双方到达所需的最少 Expand/Raid 次数之差不超过 1；
- 优先处于与当前 Beacon 不同的南北半环；
- 三个候选之间应体现不同战略方向，而不是相邻的近似选项。

移动不改变目标所有权，所以玩家可以提前判断移动后谁暂时占优。

## 11. Saltkin AI

Saltkin 每两个回合公开一张意图卡。意图卡只包含区域和意图：

- EXPAND：准备向目标区域扩张；
- RAID：准备攻击目标区域；
- DEFEND：准备保护目标区域；
- BEACON：准备争夺当前或即将出现的 Beacon。

区域只使用 NORTH、SOUTH、BEACON、CAPITAL。

示例：

> RAID · NORTH  
> Saltkin 将在北线投入至少一个 Raid。

约束：

- Saltkin 每回合至少一个命令必须符合公开意图；
- 若公开意图已经没有合法命令，界面显示 `INTENT BROKEN`；
- AI 不能读取玩家本回合尚未公开的命令；
- 大模型只从程序生成的合法意图卡中选择；
- 具体两个命令由确定性规划器生成并由规则引擎验证。

AI 的策略价值来自可读但不完整的信息：玩家知道 Saltkin 大致想做什么，但不知道具体目标和第二个命令。

## 12. 同时行动裁定表

| 情况 | 结果 |
|---|---|
| 双方 Expand 同一中立格 | 保持中立，双方命令消耗 |
| 连续 Expand 的第一步冲突 | 依赖第一步的第二次 Expand 失效 |
| 1 Raid 对无 Guard 普通地 | 攻方直接占领 |
| 1 Raid 对 1 Guard 普通地 | 防守成功 |
| 2 Raid 对 1 Guard 普通地 | 攻方直接占领 |
| 2 Raid 对无 Guard 首都 | 首都失守 |
| 2 Raid 对 1 Guard 首都 | 首都守住 |
| 相邻双方同时 Raid 对方地块 | 两块地交换所有权 |
| Raid 夺地后原主人安排 Expand | Expand 不可指向敌方地，提交时即不合法 |
| Raid 夺地后新主人本回合安排 Expand | 不能使用尚未夺取的地作提交时来源，因此不合法 |
| Beacon 在 Raid 后易手 | 新主人在本回合计分 |
| Beacon 计分后迁移 | 新位置本回合不重复计分 |

## 13. 为什么不再需要专门反僵局

旧循环通常来自：

```text
攻击 → 地块变中立 → 双方 Expand 回原边界 → 局面复原
```

新版将其改为：

```text
Raid 成功 → 所有权直接翻转
```

夺回该地必须花 Raid，而不是用 Expand 免费恢复。防守也从永久堆叠改为每回合主动投入 Guard，因此不会在环的两端逐渐形成永久高墙。

即使双方连续 Guard、互换地块或不改变地图：

- 控制 Beacon 的一方仍每回合得分；
- Beacon 每两回合迁移，改变下一阶段的投入方向；
- 每回合只有两个命令，Guard 会牺牲推进机会；
- 第 10 回合必然结算胜负。

这些是全局规则，不需要判断玩家是否进入某种特定循环。

## 14. UI 必须直接表达

- 两个秘密命令槽；
- Expand、Raid、Guard 三个按钮；
- 每个目标当前受到的 Raid 数与 Guard 数；
- 首都需要 2 个有效 Raid；
- 当前 Beacon 的预测得分者；
- Beacon 下一次迁移位置；
- Saltkin 当前公开意图及剩余回合。

核心结果不能只藏在 tooltip 中。

## 15. 原型验证

首轮至少运行 30 局机器人模拟和 10 局真人试玩，记录：

- 平均对局长度，目标 6～9 回合；
- 到达第 10 回合的比例，目标低于 25%；
- 连续两个回合结束时地图所有权完全相同的比例；
- 相邻两格反复交换所有权的次数；
- Beacon、首都与回合上限三类胜利占比；
- Guard 使用率及其成功抵消 Raid 的比例；
- 玩家第一次对局前能否正确解释 1 Raid、1 Guard、2 Raid 的结果；
- 玩家能否根据一次 Saltkin 意图主动改变决策。

若重复率仍然过高，优先调整 Beacon 迁移候选和获胜分数；不首先添加新事件、资源或状态。

## 16. 默认参数

- 每回合命令：2；
- Beacon 获胜分数：4；
- 最大回合：10；
- Beacon 迁移：每 2 回合；
- 普通地占领：1 个有效 Raid；
- 首都占领：2 个有效 Raid；
- 每目标 Guard 上限：1；
- Saltkin 意图持续：2 回合。
